<UserControl x:Class="CustomGraphicsRedactor.User_Controls.NumberBoxControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomGraphicsRedactor.User_Controls"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox x:Name="NumberHolder"
                 Text="0"
                 Grid.Row="0"
                 Grid.Column="0"
                 PreviewTextInput="NumberHolderPreviewTextInput"
                 TextChanged="NumberHolderTextChanged"/>
    </Grid>
</UserControl>
